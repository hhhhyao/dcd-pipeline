Use `skills/dcd-vis/SKILL.md` as the source of truth.

This command should stay thin:

- follow that skill end-to-end
- do not duplicate workflow logic here
- ask only when target dataset resolution is genuinely ambiguous
